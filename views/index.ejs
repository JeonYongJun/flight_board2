<!DOCTYPE html>
<html lang="ko">
  <head>
  <% include ./head.ejs %>
  </head>
  <body>
    <% include ./menu.ejs %>
    <div class="container">

      <div class="starter-template">
        <h2><%= title %></h2>
      </div>
      <form class="form-signin" action="/login" method="post">
        <h2 class="form-signin-heading">Please sign in</h2>
        <% if(message!=''){ %>
          <div class="alert alert-danger" role="alert">
            <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
            <span class="sr-only">Error:</span>
            <%= message %>
          </div>
        <% } %>
        <label for="userid" class="sr-only">Groupware User Id</label>
        <input type="text" name="userid" id="userid" class="form-control" placeholder="Groupware User Id" required="" autofocus="">
        <label for="password" class="sr-only">Groupware Password</label>
        <input type="password" name="password" id="password" class="form-control" placeholder="Groupware Password" required="">
        <div class="checkbox">
          <label>
            <input type="checkbox" id="remember_id" value="remember-me"> Remember me
          </label>
        </div>
        <button class="btn btn-lg btn-primary btn-block" id="submit_form" type="submit">Sign in</button>
      </form>
    </div><!-- /.container -->
    <footer class="footer">
      <div class="container">
        <p class="text-muted">이스타항공 정비본부, 시스템 관리자 : 오영진(puniculla@eastarjet.com,8093)
          .</p>
      </div>
    </footer>
    <script type="text/javascript">
      //$(document).ready(function() {
        var remember_id = localStorage.getItem('login_id');
        //console.log(remember_id);
        if(remember_id != null){
          $('#userid').val(remember_id);
          $('#remember_id').attr("checked", true);
        }
      //});
      $('#submit_form').on('click',()=>{
        if($('#remember_id').is(':checked')){
          localStorage.setItem('login_id',$('#userid').val());
        }else{
          localStorage.removeItem('login_id');
        }
        
        //localStorage.set()
      });
    </script>
  </body>
  <% include ./tail.ejs %>
</html>
